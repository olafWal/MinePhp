{% extends '::base.html.twig' %}

{% block content %}


    <div class="row">
        <div class="col-sm-12">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>{{ 'table.servers.head.name' | trans | desc('Name') }}</th>
                    <th>{{ 'table.servers.head.description' | trans | desc('Description') }}</th>
                    <th>{{ 'table.servers.head.actions' | trans | desc('Actions') }}</th>
                </tr>
                </thead>
                <tbody>
                {% for server in servers %}
                    <tr>
                        <td>{{ server.name }}</td>
                        <td>{{ server.description }}</td>
                        <td><a href="{{ path('app_admin_serveredit', {'id': server.id}) }}"
                               class="btn btn-primary btn-sm">{{ icon('pencil') }}</a>
                            <a class="btn btn-danger btn-sm">{{ icon('trash') }}</a></td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <td colspan="2"></td>
                    <td><a href="{{ path('app_admin_serveredit') }}" class="btn btn-primary btn-sm">{{ icon('plus') }}</a></td>
                </tr>
                </tfoot>
            </table>
        </div>
    </div>
{% endblock content %}


