{% extends '::base.html.twig' %}

{% block content %}


    <div class="row">
        <div class="col-sm-12">
            <table class="table table-striped">
                <thead>
                <tr>
                    <td>{{ 'table.servers.head.name' | trans | desc('Name') }}</td>
                    <td>{{ 'table.servers.head.description' | trans | desc('Description') }}</td>
                    <td>{{ 'table.servers.head.actions' | trans | desc('Actions') }}</td>
                </tr>
                </thead>
                <tbody>
                {% for server in servers %}
                    <tr>
                        <td>{{ server.name }}</td>
                        <td>{{ server.description }}</td>
                        <td><a href="{{ path('app_admin_serveredit', {'id': server.id}) }}"
                               class="btn btn-primary btn-sm"><span class="glyphicon glyphicon-pencil"></span></a>
                            <a class="btn btn-danger btn-sm"><span class="glyphicon glyphicon-trash"></span></a></td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <td colspan="2"></td>
                    <td><a href="{{ path('app_admin_serveredit') }}" class="btn btn-primary btn-sm"><span
                                    class="glyphicon glyphicon-plus"></span></a></td>
                </tr>
                </tfoot>
            </table>
        </div>
    </div>
{% endblock content %}


